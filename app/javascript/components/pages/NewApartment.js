import React from "react"
import PropTypes from "prop-types"

class NewApartment extends React.Component {
    constructor(props){
        super(props)
        this.state = {
            apartment: {name, street1, street2, city, state, zip, country}
        }
    }

  render () {
    return (
      <React.Fragment>
        <div>
        
        </div>
      </React.Fragment>
    );
  }
}

export default NewApartment
